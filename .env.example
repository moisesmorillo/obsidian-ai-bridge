# Local development defaults. Do not put credentials in this file.
WORKER_ENVIRONMENT=development
CLOUDFLARE_ACCOUNT_ID=your-account-id

# The R2 bucket is configured in apps/worker/wrangler.jsonc.
# Configure the Worker secret with:
# bunx wrangler secret put OBSIDIAN_BRIDGE_TOKEN
# For local development, place the value in apps/worker/.dev.vars instead.
