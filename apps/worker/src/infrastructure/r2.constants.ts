/** Content type attached to note objects stored by the R2 adapter. */
export const R2_NOTE_CONTENT_TYPE = "text/markdown; charset=utf-8";

/** R2 key namespace reserved for non-public vault note objects. */
export const VAULT_OBJECT_PREFIX = "vault/";
