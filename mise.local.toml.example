# Copy to mise.local.toml for machine-specific overrides and credentials.
# The file is ignored by Git and must never contain shared secrets.

[env]
# OBSIDIAN_BRIDGE_TOKEN = "replace-with-a-local-development-token"
